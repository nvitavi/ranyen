<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/base.xml" />
    <springProfile name="local">
        <logger name="com.ranyen" level="DEBUG" additivity="false">
            <appender-ref ref="CONSOLE" />
        </logger>
    </springProfile>
    <springProfile name="production">
	    <logger name="com.ranyen.dao" level="ERROR" additivity="false">
	        <appender-ref ref="FILE" />
	    </logger>
    </springProfile>
    <if condition='property("spring.profiles.active").equals("local")'>
        <then>
            <logger name="org.springframework.data.repository" level="INFO" additivity="false">
                <appender-ref ref="CONSOLE" />
            </logger>
        </then>
        <else>
            <logger name="org.springframework.data.repository" level="ERROR" additivity="false">
                <appender-ref ref="CONSOLE" />
            </logger>
        </else>
    </if>
</configuration>